import 'package:flutter/material.dart';

class UploadDescription extends StatelessWidget {
  const UploadDescription({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
